name: IQonomy-RDP-Robust

on:
  workflow_dispatch:

jobs:
  setup-rdp:
    runs-on: windows-latest
    steps:
    - name: Enable Remote Desktop
      run: |
        # Enable RDP and ignore errors if already enabled
        reg add "HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Terminal Server" /v fDenyTSConnections /t REG_DWORD /d 0 /f
        netsh advfirewall firewall set rule group="remote desktop" new enable=Yes
        reg add "HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" /v UserAuthentication /t REG_DWORD /d 1 /f
        
        # Create user and ignore error if it already exists (|| ver > nul)
        net user runneradmin "B9o6P2M0" /add || ver > nul
        net localgroup administrators runneradmin /add || ver > nul

    - name: Install Ngrok via Chocolatey
      run: |
        choco install ngrok -y

    - name: Authenticate Ngrok
      env:
        NGROK_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
      run: |
        ngrok config add-authtoken $env:NGROK_TOKEN

    - name: Start RDP Tunnel
      run: |
        # Force start ngrok in a new process
        Start-Process -FilePath "ngrok" -ArgumentList "tcp --region eu 3389" -NoNewWindow
        Start-Sleep -Seconds 20

    - name: Get RDP Address
      run: |
        $response = Invoke-RestMethod -Uri http://localhost:4040/api/tunnels
        $url = $response.tunnels[0].public_url
        $clean_url = $url.Replace("tcp://", "")
        Write-Host "=========================================="
        Write-Host "RDP ADDRESS : $clean_url"
        Write-Host "USERNAME    : runneradmin"
        Write-Host "PASSWORD    : B9o6P2M0"
        Write-Host "=========================================="

    - name: Keep Alive
      run: |
        Write-Host "Keep alive started... Enjoy your RDP!"
        Start-Sleep -Seconds 21600
